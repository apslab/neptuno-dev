- content_for(:breadcrumbs) do
  = breadcrumbs do |breadcrumb|
    - breadcrumb.item current_company.name.titlecase, root_path
    - breadcrumb.item t('scaffold.actions.back').titlecase, customers_path
    - breadcrumb.item t('scaffold.actions.new', :item => Customer.model_name.human).titlecase , customers_path, :active => true

= render 'form'

